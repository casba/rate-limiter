{
  "name": "rate-limited",
  "version": "1.0.0",
  "description": "Simple app to try out rate-limiting schemes.",
  "scripts": {
    "start": "node ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.4"
  },
  "nodemonConfig": {
    "ignore": [
      "test/*",
      "docs/*"
    ],
    "delay": "2500"
  }
}
